#include <stdlib.h>
#include <stdio.h>

int main() {
    // best way to hold any positive value for the array count
    size_t num_elements = 10;
    int* array = malloc(num_elements * sizeof(int));

    return 0;
}